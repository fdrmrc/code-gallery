\chapter{Polytopic Mesh DG Solver for Poisson }
\hypertarget{index}{}\label{index}\index{Polytopic Mesh DG Solver for Poisson@{Polytopic Mesh DG Solver for Poisson}}
\label{index_md__r_e_a_d_m_e}%
\Hypertarget{index_md__r_e_a_d_m_e}%


This program solves a Poisson problem on an agglomerated polytopal mesh using a symmetric interior penalty discontinuous Galerkin (SIPDG) method. Agglomerates are constructed by an R-\/tree based spatial indexing strategy, following the approach proposed in \+[2]\+. In addition, a graph-\/based METIS partitioner is also provided in this program for comparison.\hypertarget{index_autotoc_md1}{}\doxysection{\texorpdfstring{Running the code:\+}{Running the code:\+}}\label{index_autotoc_md1}
As in the tutorial programs, type

{\ttfamily cmake -\/DDEAL\+\_\+\+II\+\_\+\+DIR=/\+path/\+to/\+deal.II .}

on the command line to configure the program. After that you can compile with {\ttfamily make} and run with either {\ttfamily make run} or using

{\ttfamily ./\+agglomeration\+\_\+poisson}

on the command line.\hypertarget{index_autotoc_md2}{}\doxysection{\texorpdfstring{Program output}{Program output}}\label{index_autotoc_md2}
Running the program produces two kinds of output:\+


\begin{DoxyItemize}
\item terminal output (text summary),
\item visualization files ({\ttfamily .vtu}).
\end{DoxyItemize}

The program prints a short summary including:\+


\begin{DoxyItemize}
\item the finite element degree ({\ttfamily FE degree});
\item the triangulation size ({\ttfamily Size of tria});
\item the agglomeration construction time (R-\/tree build time);
\item the number of agglomerated subdomains ({\ttfamily N subdomains});
\item the number of Do\+Fs per cell;
\item the assembly time;
\item a convergence table with {\ttfamily \#Do\+Fs}, {\ttfamily L2 error}, and {\ttfamily H1 error}.
\end{DoxyItemize}

The program writes the following {\ttfamily .vtu} files for each run:\+


\begin{DoxyItemize}
\item {\ttfamily grid\+\_\+input\+\_\+mesh.\+vtu}, containing the input mesh from gmsh;
\item {\ttfamily grid\+\_\+fine\+\_\+mesh\+\_\+refined.\+vtu}, containing the globally refined fine mesh;
\item {\ttfamily grid\+\_\+\texorpdfstring{$<$}{<}partitioner\texorpdfstring{$>$}{>}\+\_\+\texorpdfstring{$<$}{<}n\+\_\+subdomains\texorpdfstring{$>$}{>}.vtu}, containing the agglomerated mesh partition information (cell-\/wise agglomeration labels);
\item {\ttfamily interpolated\+\_\+solution\+\_\+\texorpdfstring{$<$}{<}partitioner\texorpdfstring{$>$}{>}\+\_\+\texorpdfstring{$<$}{<}n\+\_\+subdomains\texorpdfstring{$>$}{>}.vtu}, containing the numerical solution interpolated to the fine grid together with agglomerate labels for visualization.
\end{DoxyItemize}

These files can be visualized in Para\+View to inspect both the agglomeration structure and the computed solution.\hypertarget{index_autotoc_md3}{}\doxysection{\texorpdfstring{Problem description:\+}{Problem description:\+}}\label{index_autotoc_md3}
We consider the Poisson problem in a bounded, simply connected domain $\Omega \subset \mathbb{R}^d $, $d = 2,3$. The strong formulation reads    \begin{align*}  -\Delta u &= f  && \text{in } \Omega, \\
           u &= u_D && \text{on } \partial\Omega,
\end{align*} where the right-\/hand side satisfies $f \in L^2(\Omega) $ and the prescribed Dirichlet data satisfy $u_D \in H^{1/2}(\partial\Omega) $.

The corresponding weak formulation is:\+ find $u \in H^1(\Omega)$ with $u = u_D $ on $\partial\Omega $ such that     \begin{equation}  \int_{\Omega} \nabla u \cdot \nabla v \,\mathrm d\mathbf{x}
  =\int_{\Omega} f\, v \,\mathrm d\mathbf{x}
  \qquad \text{for all } v \in H_0^1(\Omega).
\end{equation}\hypertarget{index_autotoc_md4}{}\doxysection{\texorpdfstring{SIPDG discretization on agglomerated polytopic meshes:\+}{SIPDG discretization on agglomerated polytopic meshes:\+}}\label{index_autotoc_md4}
We discretize the weak formulation by a symmetric interior penalty discontinuous Galerkin (SIPDG) method on the agglomerated polytopal mesh $T_h $, whose elements $K \in T_h $ are mutually disjoint open polygons (for $d=2 $) or polyhedra (for $d=3 $). For each element we denote its diameter by   \begin{align*}  h_K := \operatorname{diam}(K).
\end{align*} The mesh skeleton is defined by   \begin{align*}  \Gamma := \bigcup_{K \in T_h} \partial K.
\end{align*} The mesh skeleton $\Gamma $ is decomposed into $(d-1)$–dimensional simplices $F $ denoting the mesh faces, shared by at most two elements. These are distinct from elemental interfaces, which are defined as the simply connected components of the intersection between the boundary of an element and either a neighboring element or $\partial \Omega $. As such, an interface between two elements may consist of more than one face, separated by hanging nodes/\+edges shared by those two elements only. We denote by $\Gamma_{\mathrm{int}}$ the union of all interior faces, and by   $  \Gamma_{\mathrm D} := \Gamma \cap \partial\Omega
$ the union of Dirichlet boundary faces.

The discrete space $V_h $ consists of element-\/wise polynomials of degree at most \$p\$ on each $K \in T_h $. For $u_h, v_h \in V_h $ we use the broken gradient $\nabla_h $ and the standard jump and average operators $[\![\cdot]\!]$ and $\{\!\!\{\cdot\}\!\!\}$ on faces.

The DG formulation reads:\+ find $u_h \in V_h $ such that    \begin{equation}  B(u_h,v_h) = l(v_h)
  \qquad \forall\, v_h \in V_h,
\end{equation} with                \begin{align*}  B(u_h,v_h)
  &=
  \int_{\Omega} \nabla_h u_h \cdot \nabla_h v_h \,\mathrm d\mathbf{x}
  \\
  &\quad
  - \int_{\Gamma}
    \Bigl(
      \{\!\!\{\nabla u_h\}\!\!\} \cdot [\![v_h]\!]
      +
      \{\!\!\{\nabla v_h\}\!\!\} \cdot [\![u_h]\!]
    \Bigr)\,\mathrm d s
  \\
  &\quad
  + \int_{\Gamma} \sigma \,[\![u_h]\!] \cdot [\![v_h]\!] \,\mathrm d s,
\end{align*} and        \begin{equation}  l(v_h)
  =
  \int_\Omega f\, v_h \,\mathrm d\mathbf{x}
  +
  \int_{\Gamma_{\mathrm D}}
    u_D \bigl(\sigma v_h - \nabla v_h \cdot \mathbf n\bigr)\,\mathrm d s.
\end{equation}

The penalty parameter is chosen as         \begin{equation}  \sigma(\mathbf x) = C_\sigma
  \begin{cases}
    \dfrac{(p+1)(p+d)}{h_K}, &
      \text{if } \mathbf x \in \partial K \cap \partial\Omega, \\[0.5em]
    \dfrac{(p+1)(p+d)}{\min\{h_K^+,h_K^-\}}, &
      \text{if } \mathbf x \in \Gamma_{\mathrm{int}},
  \end{cases}
\end{equation} where $h_K^\pm $ are the diameters of the two elements sharing the interior face, and we fix $C_\sigma = 10 $ in this program. Here \$h\+\_\+\+K\$ in for the rtree can be regarded as

This scheme is well posed and admits optimal-\/order a priori error estimates. More precisely, assuming that $u|_K \in H^{s+1}(K) $ for all $K \in T_h $ and some $1 \le s \le p $, there exists a constant $C > 0 $, independent of $h $, such that    \[  \|u - u_h\|_{L^2(\Omega)}
  \le C\, h^{s+1} \, |u|_{H^{s+1}(\Omega)},
\] and    \[  \|\nabla(u - u_h)\|_{L^2(\Omega)}
  \le C\, h^{s} \, |u|_{H^{s+1}(\Omega)}.
\] We refer to\texorpdfstring{$\sim$}{\string~}\+[1]\+ for details of the analysis.\hypertarget{index_autotoc_md5}{}\doxysection{\texorpdfstring{Agglomeration strategies}{Agglomeration strategies}}\label{index_autotoc_md5}
Agglomeration is a natural mechanism for constructing polytopic meshes. This program supports two strategies for generating agglomerates, corresponding to the choices {\ttfamily metis} and {\ttfamily rtree}. In this example, we mainly focus on the {\ttfamily rtree} strategy, which is the method developed and introduced in the work \+[2]\+.\hypertarget{index_autotoc_md6}{}\doxysubsection{\texorpdfstring{R-\/tree-\/based agglomeration}{R-\/tree-\/based agglomeration}}\label{index_autotoc_md6}
\hypertarget{index_autotoc_md7}{}\doxysubsubsection{\texorpdfstring{Basic idea and data structure}{Basic idea and data structure}}\label{index_autotoc_md7}
In the {\ttfamily rtree} option, axis-\/aligned bounding boxes of all fine cells are inserted into a spatial R-\/tree. Agglomerates are obtained by grouping the cells whose bounding boxes belong to the same node at a user-\/selected extraction level of the tree. This purely geometric strategy does not require external graph partitioners and is typically fast and scalable. The number and shape of the agglomerates are determined by the R-\/tree structure and the chosen level.

This approach is particularly suitable for multilevel methods, where a {\itshape nested} agglomerated hierarchy is desirable.

At the data-\/structure level, we distinguish leaf nodes and internal nodes:\+


\begin{DoxyItemize}
\item {\bfseries{Leaf nodes}} store the geometric objects (here, mesh cells or their bounding boxes).
\item {\bfseries{Internal nodes}} store:\+
\begin{DoxyItemize}
\item a pointer (or reference) to a child node,
\item a bounding box that encloses all entries contained in that child subtree.
\end{DoxyItemize}
\end{DoxyItemize}

As a result, each internal node represents a spatial grouping of the objects below it.\hypertarget{index_autotoc_md8}{}\doxysubsubsection{\texorpdfstring{Design targets}{Design targets}}\label{index_autotoc_md8}
The R-\/tree is used here as a geometry-\/aware structure for organizing cell bounding boxes into hierarchical groups. Our construction is guided by the classical R\texorpdfstring{$\ast$}{*}-\/tree criteria of Beckmann et al. \+[3]\+, namely:\+
\begin{DoxyItemize}
\item {\bfseries{Minimize box area}}:\+ reduce the area covered by each bounding box,
\item {\bfseries{Minimize overlap}}:\+ reduce overlap between neighboring boxes,
\item {\bfseries{Improve shape-\/regularity}}:\+ reduce box perimeters (equivalently, favor more shape-\/regular boxes).
\end{DoxyItemize}

These criteria improve the spatial quality of the hierarchy and typically lead to better grouping and query behavior.

To make the R-\/tree construction more intuitive, we first illustrate the relation between geometric objects, their minimum bounding rectangles (MBRs), and the corresponding R-\/tree hierarchy. The left image shows geometric objects together with their enclosing MBRs, while the right image shows the associated tree structure (leaf and internal nodes). This visual example helps explain how the hierarchical grouping is later used to extract agglomerates.

   ~\newline
 {\itshape (1) Examples of MBRs holding geometric data and their MBRs} {\itshape (2) Corresponding R-\/tree data structure elements} ~\newline
 \hypertarget{index_autotoc_md9}{}\doxysubsubsection{\texorpdfstring{Agglomeration extraction}{Agglomeration extraction}}\label{index_autotoc_md9}
The construction of an R-\/tree spatial index on an arbitrary fine grid provides a natural agglomeration strategy with the following features:\+


\begin{DoxyItemize}
\item it is fully automated, robust, and dimension-\/independent;
\item it produces a nested hierarchy of agglomerates;
\item the resulting agglomerates are closely aligned with their axis-\/aligned bounding boxes.
\end{DoxyItemize}

These properties make the R-\/tree approach an attractive alternative to graph-\/based agglomeration methods.

Given a collection of fine-\/level cells, or of cut-\/cell bounding boxes, the agglomeration procedure is as follows:\+


\begin{DoxyItemize}
\item {\bfseries{Step 1:\+ Build the R-\/tree}} ~\newline
 Construct an R-\/tree from the set of bounding boxes associated with the fine-\/level cells.
\item {\bfseries{Step 2:\+ Select a target level}} ~\newline
 Choose a tree level \$l \textbackslash{}in \{1,\textbackslash{}dots,L\}\$ to control the agglomeration granularity.
\item {\bfseries{Step 3:\+ Collect leaf descendants}} ~\newline
 For each node on level \$l\$, recursively traverse its children until leaf nodes are reached.
\item {\bfseries{Step 4:\+ Agglomerate by common ancestor}} ~\newline
 Merge leaf cells that belong to the same subtree, that is, those sharing the same ancestor at level \$l\$.
\end{DoxyItemize}

This yields a nested hierarchy with natural parent-\/child relations across levels and, by repeating the extraction at different levels, produces a sequence of nested agglomerated meshes that can be used in multilevel solvers and preconditioners.

For the agglomeration workflow considered here, the R-\/tree-\/based extraction has the following practical features:\+


\begin{DoxyItemize}
\item {\bfseries{Level-\/independent extraction cost (observed):\+}} the wall-\/clock time is approximately constant with respect to the chosen extraction level.
\item {\bfseries{Boost.\+Geometry backend:\+}} the implementation relies on the {\ttfamily Boost.\+Geometry} R-\/tree.
\item {\bfseries{Custom traversal logic:\+}} the hierarchy traversal required for agglomeration is not directly exposed, so a custom node visitor is implemented.
\end{DoxyItemize}

In the present setting, this makes the R-\/tree approach particularly convenient for constructing nested agglomerated meshes in multilevel finite element and DG settings.

For illustration, the following images show R-\/tree-\/based agglomeration on a structured fine mesh:\+

   ~\newline
 {\itshape (3) Original 8x8 mesh} {\itshape (4) Bounding boxes and mesh elements} ~\newline
  ~\newline
 {\itshape (5) Tree hierarchy:\+ root node, internal nodes, and leaf nodes from top to bottom (only two child subtrees of the root are shown for clarity)} ~\newline
 

Figures (3)-\/(5) show, respectively, the original fine mesh, the blocks induced by the R-\/tree on the cell bounding boxes, and the corresponding tree structure. \hypertarget{index_autotoc_md10}{}\doxysubsection{\texorpdfstring{METIS-\/based partitioning}{METIS-\/based partitioning}}\label{index_autotoc_md10}
In the {\ttfamily metis} option, the adjacency graph of the fine mesh is constructed with one vertex per cell and edges between face-\/neighboring cells. This graph is then partitioned by the multilevel graph partitioner METIS into a prescribed number of parts, and each part defines one agglomerate; see \+[4]\+ for details.\hypertarget{index_autotoc_md11}{}\doxysection{\texorpdfstring{Test case:\+}{Test case:\+}}\label{index_autotoc_md11}
We consider the Poisson problem on the unit square $\Omega = (0,1)^2 $ with the manufactured exact solution   \[  u(x,y) = \sin(\pi x)\sin(\pi y).
\] The corresponding right-\/hand side is   \[  f(x,y) = 2\pi^2 \sin(\pi x)\sin(\pi y).
\] This manufactured solution allows us to compute the global $L^2 $-\/ and $H^1 $-\/seminorm errors of the discrete solution in order to assess the quality of the numerical approximation.

In this example, we start from an unstructured initial mesh and then perform five global refinement steps. The resulting meshes are shown below.    ~\newline
 {\itshape (6) Input mesh} {\itshape (7) Fine level mesh obtained by five global refinements of the input mesh }  Agglomerates are then constructed from the fine level mesh by METIS and by the R-\/tree strategy, leading to different polytopal meshes. The following images compare the resulting agglomerates corresponding to two different agglomeration levels (91 and 364 agglomerates).

\doxysubsubsubsection*{Comparison of agglomeration strategies}

   ~\newline
 {\itshape (8) METIS, 91 agglomerates} {\itshape (9) METIS, 364 agglomerates} ~\newline
 

   ~\newline
 {\itshape (10) R-\/tree, 91 agglomerates } {\itshape (11) R-\/tree, 364 agglomerates} ~\newline
 

These plots illustrate how the two strategies distribute and shape the agglomerates starting from the same fine level mesh. In particular, the R-\/tree approach produces geometry-\/driven groupings induced by the spatial hierarchy, while METIS produces graph-\/based partitions of the cell adjacency graph.

To assess the discretization accuracy, we next compare the error behavior under mesh refinement or increasing the polynomial degree. The plots below are obtained by collecting the program outputs over multiple runs and post-\/processing the reported error data.

  ~\newline
 {\itshape (12) h-\/convergence for (Q\+\_\+p) elements ((p=1,2,3)) with METIS and R-\/tree agglomeration} 

The figure reports the (L\texorpdfstring{$^\wedge$}{\string^}2)-\/ and (H\texorpdfstring{$^\wedge$}{\string^}1)-\/seminorm errors with respect to the manufactured solution. Optimal convergence rates are observed for all polynomial degrees and for both agglomeration strategies. In addition, the curves associated with the R-\/tree approach are marginally lower than or comparable to those obtained with METIS-\/based partitioning. Below, we provide a more detailed comparison of p-\/convergence, including both the $Q_p $ versus $P_p $ element choice and the R-\/tree versus METIS agglomeration strategies.   ~\newline
 {\itshape (13) p-\/convergence results for different element types and agglomeration strategies} 

In addition to accuracy, the cost of constructing the agglomerated polytopal meshes is also relevant in practice. The following timing plot compares the wall-\/clock time required by the R-\/tree and METIS strategies. The timing values are collected from the program outputs and summarized in post-\/processing.

  ~\newline
 {\itshape (14) Wall-\/clock time (seconds) for building polytopal grids with R-\/tree and METIS} 

Finally, the program also outputs VTU files for visualization in Para\+View. Figures (15) and (16) show the solution computed on 91 agglomerates and then interpolated back onto the fine mesh, for the R-\/tree and METIS strategies, respectively, visualized in Para\+View using Warp By Scalar together with the Surface representation.

   ~\newline
 {\itshape (15) Interpolated solution with R-\/tree strategy} {\itshape (16) Interpolated solution with METIS strategy} \hypertarget{index_autotoc_md12}{}\doxysection{\texorpdfstring{References}{References}}\label{index_autotoc_md12}

\begin{DoxyItemize}
\item \+[1]\+ Di Pietro, Daniele Antonio and Ern, Alexandre (2012), Mathematical Aspects of Discontinuous Galerkin Methods. ISBN:\+ \href{https://www.springer.com/gp/book/9783642229794}{\texttt{978-\/3-\/642-\/22980-\/0}}
\item \+[2]\+ Marco Feder, Andrea Cangiani and Luca Heltai (2025), R3\+MG:\+ R-\/tree based agglomeration of polytopal grids with applications to multilevel methods. DOI:\+ \href{https://doi.org/10.1016/j.jcp.2025.113773}{\texttt{10.\+1016/\+j.jcp.\+2025.\+113773}}
\item \+[3]\+ Beckmann, Norbert; Kriegel, Hans-\/\+Peter; Schneider, Ralf; Seeger, Bernhard (1990), The R\texorpdfstring{$\ast$}{*}-\/tree:\+ an efficient and robust access method for points and rectangles. DOI:\+ \href{https://doi.org/10.1145/93597.98741}{\texttt{10.\+1145/\+93597.98741}}
\item \+[4]\+ George Karypis and Vipin Kumar (1998), A fast and high quality multilevel scheme for partitioning irregular graphs. DOI:\+ \href{https://doi.org/10.1137/S1064827595287997}{\texttt{10.\+1137/\+\+S1064827595287997}} 
\end{DoxyItemize}